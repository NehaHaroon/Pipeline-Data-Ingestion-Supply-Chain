apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    # Grafana runs inside Docker — use service name + container port 9090.
    url: http://pipeline-prometheus:9090
    isDefault: true
    editable: false
