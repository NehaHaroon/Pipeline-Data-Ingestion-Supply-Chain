name: supply_chain_semantic
version: "1.0.0"
config-version: 2
profile: supply_chain

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - target
  - dbt_packages

vars:
  semantic_parquet_dir: "../../../storage/semantic/parquet"
  fx_pkr_per_usd: 278.0

models:
  supply_chain_semantic:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
